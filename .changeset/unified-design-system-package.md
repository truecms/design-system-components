---
"@truecms/design-system": minor
---

Introduce the unified Vite-based GovAU design system package that produces React components and Drupal-compatible CSS bundles from a single build, and wire npm release workflows so the unified package can be built, tested, and published alongside existing component packages.

