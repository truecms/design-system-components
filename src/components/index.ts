/**
 * Unified React components entry point.
 *
 * Exposes the compatibility layer that preserves the legacy GovAU
 * React components' public APIs while allowing consumers to migrate
 * to the unified package by changing import paths only.
 */

export * from './compat/legacyExports';
